# === WSL CORS Configuration ===
# Add your WSL IP address to CORS origins
# Get WSL IP: hostname -I | awk '{print $1}'
CORS_ORIGIN=http://localhost:3001,http://localhost:5173,http://YOUR_WSL_IP:5173

# Redis Configuration
# Use your own Redis instance or the shared cloud instance
REDIS_URL=redis://localhost:6379

# Engine Configuration
PORT=3000
ENGINE_ID=cursor-engine
IS_PRIMARY=false

# Node Environment
NODE_ENV=development

# Admin Dashboard
ADMIN_PORT=3001

# Player Dashboard URLs (for WSL, use WSL IP)
VITE_ENGINE_URL=http://YOUR_WSL_IP:3000

# LLM API Keys (required for AI features)
ANTHROPIC_API_KEY=your_anthropic_key_here
OPENAI_API_KEY=your_openai_key_here
